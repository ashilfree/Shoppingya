{% extends 'base.html.twig' %}

{% block body %}

    <!--================Categories Banner Area =================-->
    <section class="solid_banner_area">
        <div class="container">
            <div class="solid_banner_inner">
                <h3>Reset Password</h3>
                <ul>
                    <li><a href="{{ path('home', { locale: 'en' }) }}">Home</a></li>
                    <li><a href="">Reset Password</a></li>
                </ul>
            </div>
        </div>
    </section>
    <!--================End Categories Banner Area =================-->

    <!--================login Area =================-->
    <section class="login_area p-t-100 p-b-100">
        <div class="container">
            <div class="login_inner">
                <div class="row justify-content-center">
                    <div class="col-md-4">
                        <div class="login_title">
                            <h2>reset password for your account</h2>
                            <p>Log in to your account to discovery all great features in this template.</p>
                        </div>
{#                        {% if error %}#}
{#                            <div class="alert alert-danger">#}
{#                                {{ error.message | trans(error.messageData, 'security') }}#}
{#                            </div>#}
{#                        {% endif %}#}
                        {{ form_start(requestForm, {'attr': {'class': 'login_form row'}}) }}
                            <div class="col-lg-12 form-group">
                                {{ form_row(requestForm.email, {'attr': {'class': 'form-control', 'placeholder':'Enter Your Email'}}) }}
                            </div>
                            <div class="col-lg-12 form-group">
                                <button type="submit" value="submit" class="btn update_btn w-full">Send</button>
                            </div>
                        {{ form_end(requestForm) }}
                    </div>
                </div>
            </div>
        </div>
    </section>
    <!--================End login Area =================-->

{% endblock %}

