<!DOCTYPE html>
<html  lang="ar-AL" dir="rtl" lang="ar">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}{% endblock %}</title>
    <!--===============================================================================================-->
    <link rel="icon" type="image/png" href="{{ '/media/images/icons/favicon.png' }}"/>

    {{ encore_entry_link_tags('app-ar') }}

    {% block stylesheets %}{% endblock %}
    {% block javascript %}
    {% endblock %}
    <!--===============================================================================================-->
</head>
<body class="animsition js-body">

<!-- Header -->
<header {% if page is defined and page != 'home.ar' %} class="header-v4" {% endif %}>
    <!-- Header desktop -->
    <div class="container-menu-desktop">
        <!-- Topbar -->
        <div class="top-bar">
            <div class="content-topbar flex-sb-m h-full container">
                <div class="left-top-bar">
                    التوصيل المجاني للطلب 20 {{ currency_ar }} أو أكثر
                </div>

                <div class="right-top-bar flex-w h-full">
{#                    <a href="#" class="flex-c-m trans-04 p-lr-25">#}
{#                        المساعدة والأسئلة الشائعة#}
{#                    </a>#}

                    {% if app.user %}
                        <a href="{{ path('account', { locale: 'ar' }) }}" class="flex-c-m trans-04 p-lr-25">
                            حسابي
                        </a>
                    {% else %}
                        <a href="{{ path('register', { locale: 'ar'}) }}" class="flex-c-m trans-04 p-lr-25">
                            مستخدم جديد
                        </a>
                    {% endif %}

                    <a href="{{ path('home', { locale: 'en' }) }}" class="flex-c-m trans-04 p-lr-25">
                        الإنجليزية
                    </a>
                    {% if app.user %}
                    <a href="{{ path('account', { locale: 'ar' }) }}" class="flex-c-m trans-04 p-lr-25">
                        {{  'مرحبا ' ~ app.user.username   }} {{ ' ' }}
                    </a>
                    {% endif %}
                    {% if app.user %}

                        <a href="{{ path('logout', { locale: 'ar' }) }}" class="flex-c-m p-lr-10 trans-04">
                            <i class="fa fa-sign-out"></i>
                        </a>
                    {% else %}
                        <a href="{{ path('login', { locale: 'ar'}) }}" class="flex-c-m p-lr-10 trans-04">

                            تسجيل الدخول
                        </a>
                    {% endif %}

                </div>
            </div>
        </div>

        <div class="wrap-menu-desktop">
            <nav class="limiter-menu-desktop container">

                <!-- Logo desktop -->
                <a href="{{ path('home', { locale: 'ar' }) }}" class="logo">
                    <img src="{{ '/media/images/icons/logo-01.png' }}" width="140" alt="IMG-LOGO">
                </a>

                <!-- Menu desktop -->
                <div class="menu-desktop">
                    <ul class="main-menu">
                        <li {% if page is defined and page == 'home.ar' %} class="active-menu" {% endif %}>
                            <a href="{{ path('home', { locale: 'ar' }) }}">الرئيسية</a>
                        </li>

                        <li {% if page is defined and page == 'about.us.ar' %} class="active-menu" {% endif %}>
                            <a href="{{ path('about.us', { locale: 'ar' }) }}">عن الشركة</a>
                        </li>

                        <li {% if page is defined and page == 'products.ar' %} class="active-menu" {% endif %}>
                            <a href="{{ path('products', { locale: 'ar' }) }}">الأقسام</a>
                        </li>


                        <li {% if page is defined and page == 'contact.us.ar' %} class="active-menu" {% endif %}>
                            <a href="{{ path('contact.us', { locale: 'ar' }) }}">اتصل بنا</a>
                        </li>
                    </ul>
                </div>

                <!-- Icon header -->
                <div class="wrap-icon-header flex-w flex-r-m js-panel-container">
                    {% include 'pages/desktop/search-icon.html.twig' %}

                    {% include 'pages/desktop/cart-icon.html.twig' %}

                    {% include 'pages/desktop/wish-list-icon.html.twig' %}
                </div>
            </nav>
        </div>
    </div>

    <!-- Header Mobile -->
    <div class="wrap-header-mobile">
        <!-- Logo moblie -->
        <div class="logo-mobile">
            <a href="{{ path('home', { locale: 'ar' }) }}"><img src="{{ '/media/images/icons/logo-01.png' }}" width="120" alt="IMG-LOGO"></a>
        </div>

        <!-- Icon header -->
        <div class="wrap-icon-header flex-w flex-r-m m-r-15">
            {% include 'pages/mobile/search-icon.html.twig' %}

            {% include 'pages/mobile/cart-icon.html.twig' %}

            {% include 'pages/mobile/wish-list-icon.html.twig' %}
        </div>

        <!-- Button show menu -->
        <div class="btn-show-menu-mobile hamburger hamburger--squeeze">
				<span class="hamburger-box">
					<span class="hamburger-inner"></span>
				</span>
        </div>
    </div>


    <!-- Menu Mobile -->
    <div class="menu-mobile">
        <ul class="topbar-mobile">
            <li>
                <div class="left-top-bar text-right">
                    التوصيل المجاني للطلب 20 {{ currency_ar }} أو أكثر
                </div>
            </li>

            <li>
                <div class="right-top-bar flex-w h-full">
{#                    <a href="#" class="flex-c-m p-lr-10 trans-04">#}
{#                        المساعدة والأسئلة الشائعة#}
{#                    </a>#}

                    {% if app.user %}
                        <a href="{{ path('account', { locale: 'ar' }) }}" class="flex-c-m p-lr-10 trans-04">
                            حسابي
                        </a>
                    {% else %}
                        <a href="{{ path('register', { locale: 'ar'}) }}" class="flex-c-m p-lr-10 trans-04">
                            مستخدم جديد
                        </a>
                    {% endif %}

                    <a href="{{ path('home', { locale: 'en' }) }}" class="flex-c-m p-lr-10 trans-04">
                        الإنجليزية
                    </a>
                    {% if app.user %}
                        <a href="{{ path('account', { locale: 'ar' }) }}" class="flex-c-m trans-04 p-lr-25">
                            {{  'مرحبا ' ~ app.user.username   }} {{ ' ' }}
                        </a>
                    {% endif %}
                    {% if app.user %}

                        <a href="{{ path('logout', { locale: 'ar' }) }}" class="flex-c-m p-lr-10 trans-04">
                            <i class="fa fa-sign-out"></i>
                        </a>
                    {% else %}
                        <a href="{{ path('login', { locale: 'ar'}) }}" class="flex-c-m p-lr-10 p-t-5 trans-04">

                            <i class="fa fa-sign-in"></i>
                        </a>
                    {% endif %}

                </div>
            </li>
        </ul>

        <ul class="main-menu-m text-right">
            <li>
                <a href="{{ path('home', { locale: 'ar' }) }}">الرئيسية</a>
            </li>

            <li>
                <a href="{{ path('about.us', { locale: 'ar' }) }}">معلومات عنا</a>
            </li>

            <li>
                <a href="{{ path('products', { locale: 'ar' }) }}">الأقسام</a>
            </li>

            <li>
                <a href="{{ path('contact.us', { locale: 'ar' }) }}">اتصل بنا</a>
            </li>
        </ul>
    </div>

    <!-- Modal Search -->
    <div class="modal-search-header flex-c-m trans-04 js-hide-modal-search">
        <div class="container-search-header">
            <button class="flex-c-m btn-hide-modal-search trans-04 js-hide-modal-search">
                <img src="{{ '/media/images/icons/icon-close2.png' }}" alt="CLOSE">
            </button>

            <form class="wrap-search-header flex-w p-l-15">
                <button class="flex-c-m trans-04">
                    <i class="zmdi zmdi-search"></i>
                </button>
                <input class="plh3" type="text" name="search" placeholder="Search...">
            </form>
        </div>
    </div>
</header>

<!-- Cart -->
<div class="wrap-header-cart js-panel-cart">
    <div class="s-full js-hide-cart"></div>

    <div class="header-cart flex-col-l p-l-65 p-r-25">
        <div class="header-cart-title flex-w flex-sb-m p-b-8">
				<span class="mtext-103 cl2">
					سلتك
				</span>

            <div class="fs-35 lh-10 cl2 p-lr-5 pointer hov-cl1 trans-04 js-hide-cart">
                <i class="zmdi zmdi-close"></i>
            </div>
        </div>
        {% set total = null %}
        <div class="header-cart-content align-content flex-w js-pscroll">
            {% if cart %}
                <ul class="header-cart-wrapitem w-full">
                    {% for cartItem in cart %}
                        <li class="header-cart-item flex-w flex-t m-b-12">
                            <a class="header-cart-item-img" href="{{ path('delete.cart', {id: cartItem.catalog.id, route:page}) }}">
                                <img src="{{ vich_uploader_asset(cartItem.catalog.product.images.first, 'imageFile') | imagine_filter('small') }} "
                                     alt="{{ cartItem.catalog.product.name }}">
                            </a>

                            <div class="header-cart-item-txt p-t-8 text-right">
                                <a href="{{ path('product', {locale: 'ar', 'slug': cartItem.catalog.product.slug }) }}" class="header-cart-item-name m-b-6 hov-cl1 trans-04">
                                    {{ cartItem.catalog.product.nameAr|upper }}
                                </a>
                                <span class="header-cart-item-info">
                                    {% if cartItem.catalog.product.discountPrice != 0 %}
								        {{ (cartItem.catalog.product.discountPrice / 100) |number_format(2, '.', ',') ~ 'x' ~ cartItem.quantity }}  {{ currency_ar }}
                                    {% else %}
                                        {{ (cartItem.catalog.product.price / 100) |number_format(2, '.', ',') ~ 'x' ~ cartItem.quantity }} {{ currency_ar }}
                                    {% endif %}
							</span>
                                <span class="header-cart-item-info">
								الحجم: <b> {{ cartItem.catalog.size }} </b>
							</span>
                            </div>
                        </li>
                        {% if cartItem.catalog.product.discountPrice != 0 %}
                            {% set total = total + (cartItem.quantity * cartItem.catalog.product.discountPrice) %}
                        {% else %}
                            {% set total = total + (cartItem.quantity * cartItem.catalog.product.price) %}
                        {% endif %}
                    {% endfor %}
                </ul>
            {% else %}
                <img class="img-responsive" src="{{ '/media/images/waiting.png' }}" width=240 alt="ICON-PAY">

            {% endif %}
            <div class="w-full">
                <div class="header-cart-total w-full p-tb-40 text-right">
                    {% if cart %}
                        مجموع: {{ (total / 100)|number_format(2) }} {{ currency_ar }}
                    {% else %}
                        <p><b>عربة التسوق فارغة</b></p>
                    {% endif %}
                </div>

                <div class="header-cart-buttons flex-w w-full">
                    <a  href="{{ path('cart', { locale: 'ar' }) }}"
                            class="flex-c-m stext-101 cl0 size-107 bg3 bor2 hov-btn3 p-lr-15 trans-04 m-l-8 m-b-10" >
                          عرض عربة التسوق
                    </a>

                    <a {% if cart|length != 0 %} href="{{ path('remove.cart', {locale:'ar', route: page}) }}" {% endif %}
                            class="flex-c-m stext-101 cl0 size-107 bg3 bor2 hov-btn3 p-lr-15 trans-04 m-b-10" {% if cart|length == 0 %} disabled="true" {% endif %}>
                         إخلاء عربة التسوق
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- Wish -->
<div class="wrap-header-cart js-panel-wish">
    <div class="s-full js-hide-wish"></div>

    <div class="header-cart flex-col-l p-l-65 p-r-25">
        <div class="header-cart-title flex-w flex-sb-m p-b-8">
                                <span class="mtext-103 cl2">
                                    قائمة امنياتك
                                </span>

            <div class="fs-35 lh-10 cl2 p-lr-5 pointer hov-cl1 trans-04 js-hide-wish">
                <i class="zmdi zmdi-close"></i>
            </div>
        </div>
        <div class="header-cart-content flex-w js-pscroll">
            {% if wishlist %}
                <ul class="header-cart-wrapitem w-full">
                    {% for cartItem in wishlist %}
                        <li class="header-cart-item flex-w flex-t m-b-12">
                            <a class="header-cart-item-img" href="{{ path('delete.wishlist', {id: cartItem.product.id, route:page, locale: 'ar'}) }}">
                                <img src="{{ vich_uploader_asset(cartItem.product.images.first, 'imageFile') | imagine_filter('small') }} "
                                     alt="{{ cartItem.product.name }}">
                            </a>

                            <div class="header-cart-item-txt p-t-8">
                                <a href="{{ path('product', {'slug': cartItem.product.slug, locale: 'ar' }) }}" class="header-cart-item-name m-b-6 hov-cl1 trans-04  text-right">
                                    {{ cartItem.product.nameAr }}
                                </a>
                            </div>
                        </li>
                    {% endfor %}
                </ul>
            {% else %}
                <img class="img-responsive" src="{{ '/media/images/waiting.png' }}" width=240 alt="ICON-PAY">

            {% endif %}

            <div class="w-full">
                <div class="header-cart-buttons flex-w w-full align-items-center">
                    <a {% if wishlist|length != 0 %} href="{{ path('remove.wishlist', {locale: 'ar', route: page}) }}" {% endif %}
                            class="flex-c-m stext-101 cl0 size-107 bg3 bor2 hov-btn3 p-lr-15 trans-04 m-b-10" {% if wishlist|length == 0 %} disabled="true" {% endif %}>
                        إفراغ القائمة
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

{% block body %}{% endblock %}

<!-- Footer -->
<footer class="bg3 p-t-75 p-b-32">
    <div class="container">
        <div class="row">
            <div class="col-sm-6 col-lg-4 p-b-50">
                <h4 class="stext-301 cl0 p-b-30 text-right">
                    الأقسام
                </h4>

                <ul>
                    {% for category in categories  %}
                        <li class="p-b-10 text-right">
                            <a href="#" class="stext-107 cl7 hov-cl1 trans-04">
                                {{ category.nameAr }}
                            </a>
                        </li>
                    {% endfor %}
                </ul>
            </div>

            <div class="col-sm-6 col-lg-4 p-b-50 text-right">
                <h4 class="stext-301 cl0 p-b-30">
                    اتصل بنا
                </h4>

                <p class="stext-107 cl7 size-201">
                    برج النصار دور 10 <br> مدينة الكويت - الكويت
                </p>

                <div class="p-t-27">
                    <a href="#" class="fs-18 cl7 hov-cl1 trans-04 m-r-16">
                        <i class="fa fa-facebook"></i>
                    </a>

                    <a href="#" class="fs-18 cl7 hov-cl1 trans-04 m-r-16">
                        <i class="fa fa-instagram"></i>
                    </a>

                    <a href="#" class="fs-18 cl7 hov-cl1 trans-04 m-r-16">
                        <i class="fa fa-youtube"></i>
                    </a>
                </div>
            </div>

            <div class="col-sm-10 col-lg-4 p-b-50 text-right">
                <h4 class="stext-301 cl0 p-b-30">
                    النشرة الإخبارية
                </h4>

                <form>
                    <div class="wrap-input1 w-full p-b-4">
                        <input class="input1 bg-none plh1 stext-107 cl7" type="text" name="email"
                               placeholder="email@example.com">
                        <div class="focus-input1 trans-04"></div>
                    </div>

                    <div class="p-t-18">
                        <button class="flex-c-m stext-101 cl0 size-103 bg1 bor1 hov-btn2 p-lr-15 trans-04">
                            إشتراك
                        </button>
                    </div>
                </form>
            </div>
        </div>

        <div class="p-t-40">
            <div class="flex-c-m flex-w p-b-18">
                <a class="m-all-1">
                    <img src="{{ '/media/images/icons/icon-pay.png' }}" width="120" alt="ICON-PAY">
                </a>
            </div>

            <p class="stext-107 cl6 txt-center">
                <!-- Link back to Colorlib can't be removed. Template is licensed under CC BY 3.0. -->
                حقوق النشر &copy;<script>document.write(new Date().getFullYear());</script>
                جميع الحقوق محفوظة | مصمم <i class="fa fa-heart-o" aria-hidden="true"></i> بواسطة <a
                        href="https://colorlib.com" target="_blank">shoow co.</a>
                <!-- Link back to Colorlib can't be removed. Template is licensed under CC BY 3.0. -->

            </p>
        </div>
    </div>
</footer>


<!-- Back to top -->
<div class="btn-back-to-top" id="myBtn">
		<span class="symbol-btn-back-to-top">
			<i class="zmdi zmdi-chevron-up"></i>
		</span>
</div>

<!--===============================================================================================-->
{{ encore_entry_script_tags('app-ar') }}

{% block javascripts %}{% endblock %}
<!--===============================================================================================-->

</body>
</html>
